import React, { Component } from 'react'

const Homepage = props => {
    
        return (
            <div>
                <h1 class="home">if(More About Me: 
                    <br></br>click the links) 
                    <br></br>else {'{come back later}'} </h1>
            </div>
        )
}

export default Homepage
